## What changed

<!-- One paragraph. What behaviour is different now? -->

## Why

<!-- The constraint that forced it. Link the ADR if there is one. -->

## Checklist

- [ ] `just check` passes
- [ ] New behaviour has a `SPEC-` clause in `docs/10-product/srs.md`, naming its test
- [ ] Any decision with a rejected alternative has an ADR
- [ ] Documentation Maintenance Protocol has run
- [ ] No credential (`.env`, `*.session`) appears in the diff
- [ ] The Send Guard was not weakened

## DOCS SYNC

```text
DOCS SYNC
  Change type:
  Updated:
  IDs added:
  IDs superseded:
  docs-check:
```
